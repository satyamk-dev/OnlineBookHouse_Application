spring.application.name=OnlineBookHouse_EurekaServer

server.port=8761

eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

eureka.instance.instance-id=${spring.application.name}:${server.port}:${randam.value}
